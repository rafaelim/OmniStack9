{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:api:dev": "lerna exec --scope @aircnc/api -- yarn start:dev",
    "start:app:dev": "lerna exec --scope @aircnc/app -- yarn start"
  },
  "dependencies": {
    "lodash": "4.17.21"
  },
  "devDependencies": {
    "lerna": "^3.16.4"
  }
}
